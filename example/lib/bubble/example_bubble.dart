import 'package:flutter/material.dart';

class ExampleBubble extends StatelessWidget {
  const ExampleBubble({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
